# Recipes

Recipes describe opinionated product patterns by combining blueprints and capabilities. They do not introduce a new runtime foundation.

Examples: commerce, messaging, automation, content platforms, and AI products.

## v1 policy

The first public catalog keeps recipes empty until the underlying capabilities and evolution paths are stable and tested. This prevents product-specific complexity from leaking into the foundation layer.
