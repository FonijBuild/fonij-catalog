{
  "name": "@fonijbuild/catalog",
  "version": "1.0.0",
  "private": true,
  "description": "Versioned architecture catalog for the FonijBuild product builder ecosystem.",
  "type": "module",
  "engines": {
    "node": ">=22.14.0"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run validate && npm test"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1"
  }
}
